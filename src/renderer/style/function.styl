reset-ul()
  list-style none
  margin 0
  padding 0
full-height()
  height 100%
full-width()
  width 100%
full-size()
  full-height()
  full-width()
fix-flex()
  flex-grow 0
  flex-shrink 0
paper()
  box-shadow 0 3px 10px rgba(0, 0, 0, 0.156863), 0 3px 10px rgba(0, 0, 0, 0.227451)
radius()
  border-radius 8px
bg-gradient-blue()
  background linear-gradient(to right, #409eff 0%, #b3d9ff 100%)

board-transitioin()
  transition all 0s
